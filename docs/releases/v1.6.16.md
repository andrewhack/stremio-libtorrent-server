## Downloads fetch the episode, not its sample

A download started from a title's page for one episode, from a release that keeps a `Sample/`
folder, could fetch the sample instead of the episode and then show the episode as downloaded. It
now fetches the episode's largest video, as playback already did, and the page's buttons and episode
ticks read that same file.

**Upgrade:** pull it.
